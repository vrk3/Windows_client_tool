# Hard requirements. tests/test_runtime_dependencies.py asserts every line
# above the first "# Optional" below is importable, so nothing may be listed
# here that the code does not actually use — pyqtgraph was, for months, while
# perfmon_charts.py said `HAS_PYQTGRAPH = False  # intentionally False`, and
# that noise is what let a genuinely missing WMI go unnoticed.
PyQt6>=6.6
psutil>=5.9
pywin32>=306
WMI>=1.5.1
requests>=2.31

# Optional — enables packet capture in Network Diagnostics; the app degrades
# gracefully without it (see network_diagnostics/network_tools.py).
scapy>=2.5

# Optional — TreeSize export formats. Excel and PDF are offered only when
# these are present; every other format needs nothing beyond the standard
# library (see modules/treesize/actions/exporters.py).
openpyxl>=3.1
reportlab>=4.0

# Optional — TreeSize remote scan targets (spec section 6). Each backend is
# offered only when its package is present, and says which one it wants when
# it is not; local drives and folders need none of them. Outlook needs only
# pywin32, which is already a hard dependency above.
#   paramiko    -> SSH / SFTP
#   httpx       -> WebDAV, and SharePoint through Microsoft Graph
#   boto3       -> AWS S3
#   azure-*     -> Azure Blob Storage
#   google-*    -> Google Drive
paramiko>=3.4
httpx>=0.27
boto3>=1.34
azure-storage-blob>=12.19
google-api-python-client>=2.120
google-auth-oauthlib>=1.4.1

# Dev / test only. Also declared in pyproject.toml's
# [project.optional-dependencies] dev extra; this file stays the one CI
# installs from.
pytest>=7.0
pytest-timeout>=2.2
pytest-xdist>=3.5
pytest-cov>=5.0
ruff>=0.5
